/**
	This is the controller that handles bookings. What is a booking?
	A booking is the users attempt to allocate himself a room for a given
	period of time.

	Although the plan is to get this page to allow users to do bookings, bookings
	are also a profile matter. The plan is to keep this as the 'dashboard' of users
	wanting to track their clients and clients to track their bookings. This is different to
	the users profile, which is there to store personal data about the user. This is to controller
	the users

**/

exports.test = function(req,res,next){
	res.end('bookings test');
}
